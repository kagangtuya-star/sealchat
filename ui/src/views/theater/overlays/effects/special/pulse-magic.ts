import type { SceneOverlayEffectDefinition } from '../../scene-overlay-types'
import { colorParam, numberParam } from '../effect-helpers'

export default { id: 'special.pulse-magic', name: '魔法脉冲', description: '蓝紫色周期魔力脉冲。', category: 'special', defaultParams: { strength: 0.58, frequency: 0.45, color: '#7655ff' }, controls: [{ type: 'number', key: 'strength', label: '亮度', min: 0.05, max: 1, step: 0.05 }, { type: 'number', key: 'frequency', label: '频率', min: 0.03, max: 2, step: 0.01, suffix: '次/秒' }, { type: 'color', key: 'color', label: '颜色' }], defaultBlendMode: 'screen', buildRenderDescriptor(params) { return { renderer: 'color', config: { mode: 'pulse', color: colorParam(params, 'color', '#7655ff'), strength: numberParam(params, 'strength', 0.58, 0.05, 1), frequency: numberParam(params, 'frequency', 0.45, 0.03, 2) } } } } satisfies SceneOverlayEffectDefinition
