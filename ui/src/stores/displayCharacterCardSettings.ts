export const normalizeCharacterCardBadgeSettingsExpanded = (value: unknown) => {
  return value !== false
}

export const normalizeOnlineCharacterCardsEnabled = (value: unknown) => {
  return value !== false
}
