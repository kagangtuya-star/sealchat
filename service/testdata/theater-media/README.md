# Theater media fixtures

Unit tests generate tiny deterministic PNG/GIF fixtures in `t.TempDir()`. Real FFmpeg integration tests run only when configured toolchain exists; tests never download media.
